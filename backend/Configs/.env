# Test Values
TESTVALUE=333
# true means you deactivate caching 
DEVELOPER=true
# Default language
DEFAULT_LANGUAGE=en
# Add a comma-separated string of allowed origins (Or let it empty, to allow all hosts route calls):
ALLOWED_ORIGINS=
# Database Connection sqlite/mysql
DB_CONNECTION=sqlite
DB_HOST=127.0.0.1
DB_PORT=3306

# If you use sqlite this will be the name of your database file ("developer.db")
DB_DATABASE=developer

# Database auth.
DB_USERNAME=a7
DB_PASSWORD=nenerr

# Cache Queries
DB_CACHING=true

DEBUG_VALUE1=68716a6548853_cmd_0
DEBUG_VALUE2=test

DEBUG_VALUE3=68716a6548853_cmd_1
DEBUG_VALUE4=12